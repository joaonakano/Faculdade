
import 'package:flutter/material.dart';

class ExpensesCreationScreen extends StatelessWidget {
  const ExpensesCreationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Lmao this screen needs to be built");
  }
}